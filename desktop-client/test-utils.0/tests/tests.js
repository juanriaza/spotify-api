/*
 * Configure things so we can import BDD style tests
 */
mocha.setup('bdd');

describe('Test Environment', function() {
  it('should provide the "describe" and "it" spec syntax', function() {});
});
